# Olympus Sovereign Agent (v1)
FROM golang:1.26-alpine AS builder

WORKDIR /app
COPY . .
RUN go build -o agent .

FROM alpine:latest
WORKDIR /app
COPY --from=builder /app/agent .
COPY AgentLease.jebnf .

ENTRYPOINT ["./agent"]
