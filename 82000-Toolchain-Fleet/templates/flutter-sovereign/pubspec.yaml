name: flutter_sovereign_template
description: "Sovereign Fleet Master UI Template"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  connectrpc: {{CONNECT_VERSION}}
  cronet_http: {{CRONET_VERSION}}
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
